jdk:
  - openjdk8
#before_install:
#  - ./custom_setup.sh
install:
  - echo "Running a custom install command"
  - ./gradlew clean build -xtest # needs to be install?
env:
  MYVAR: "custom environment variable"
