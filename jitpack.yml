jdk: openjdk11
#before_install:
#  - ./custom_setup.sh
install:
  - echo "Running a custom install command"
  # - ./gradlew clean build -xtest  # needs to be install?
  - ./gradlew clean :FlagSmithClient:compileDebugKotlin :FlagSmithClient:compileDebugKotlin -xtest
  - echo "Copying the build output"
  - ls -al
  - pwd
  - cp -r ./FlagsmithClient/build .
env:
  MYVAR: "custom environment variable"
